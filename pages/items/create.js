import AppTitle from "@/components/title";
import AddEditItem from "@/components/addEditItem";

const CreateItem = () => {
  return (
    <div>
      <AppTitle level={2}>Create Item</AppTitle>
      <AddEditItem />
    </div>
  );
};
export default CreateItem;
