import AppTitle from "@/components/title";
import CreateQuotation from "@/components/createQuotation";

const CreateQuotationPage = () => {
  return (
    <div>
      <AppTitle level={2}>Create Quotation</AppTitle>
      <CreateQuotation />
    </div>
  );
};

export default CreateQuotationPage;
